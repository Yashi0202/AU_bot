services:
  - type: web
    name: kuber-yashi
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    # Environment variables must be set in Render dashboard:
    # 1. Go to your service settings > Environment tab
    # 2. Add MONGO_URI: mongodb+srv://abhiram:ebGBhxU5cVvDHqAo@nodeexpressprojects.diw08.mongodb.net/test?retryWrites=true&w=majority
    # 3. Add OPENAI_API_KEY: your-openai-api-key (optional)
